#include "tree.h"
#include <iostream>
/*
typedef struct {} node;

//template<class DataTypeKey, class DataTypeValue, class StructType>
virtual Tree<int, int, node>::node *Tree<int, int, node>::CreateNode(int key, int value) { 
	node *node;
	try {
		node = new node;
	} catch (bad_alloc xa) {
		cout << "\t\nBad allocation for node.\n";
		exit(1);
	}
	node->key = key;
	node->value = value;
	node->left = NULL;
	node->right = NULL;

	return node; 
}*/